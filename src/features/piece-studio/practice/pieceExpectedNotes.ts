/** Re-export from score — expected notes are score-derived, not capture domain. */
export {
  expectedMidisFromScore,
  expectedNotesFromScore,
  type PieceExpectedNote,
} from "@/features/piece-studio/score/expectedNotes";
